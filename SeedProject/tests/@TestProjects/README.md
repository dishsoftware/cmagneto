# Test Projects

This directory contains fixture projects used by system-level tests.

These projects are meant to mimic downstream consumers or other external
integration scenarios without being part of the normal `tests/native/` CMake
subtree.
